# `cti` Functional Tests

## Running tests from a tests rpm

```
$ ./test_tool.py --cp <destination> # if tests are installed in a directory that root owns
$ ./test_tool.py
```

## Running tests from a git clone

```
$ git submodule update --init ./cdst-test
$ ./test_tool.py
```

