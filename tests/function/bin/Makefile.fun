# Copyright 2020 Hewlett Packard Enterprise Development LP.

# The binaries in this directory are created on a the build machine and packaged
# in the test rpm with no source.   However, since the 'make' structure is expecting
# a make here, we need a trivial makefile for the rpm that won't delete the existing
# binaries

all:

install:

clean:

